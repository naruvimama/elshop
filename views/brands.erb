<span class="btn">
    <i class="icon-plus"></i>
    <a href="/admin/new/brand">Add brand</a>
</span>
<div class="msg_box"></div>
<ul>
	<% @brands.each do |brand| %>
		<li>
			<%= "#{brand.name} - (#{brand.appliances_count}) appliances" %>
			<a class='delete_brand' href="#" data_id=<%= brand.id %> > Delete </a>
			<a class='edit_brand' href=<%= "/admin/edit/brand/#{brand.id}" %> > Edit </a>
		</li>
	<%end%>
</ul>
